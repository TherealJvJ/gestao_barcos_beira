name: mobile_app
description: "Aplicação Móvel para Gestão de Barcos — INTRANSMAR Beira"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  http: ^1.1.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.0.5
  intl: ^0.18.1
  url_launcher: ^6.1.12
  share_plus: ^7.1.0
  path_provider: ^2.1.1
  open_file: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
